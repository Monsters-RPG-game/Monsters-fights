export enum EFightsTargets {
  Attack = 'attack',
  CreateFight = 'createFight',
  UseSkill = 'useSkill',
  Leave = 'leave',
  GetLogs = 'getLogs',
  GetFights = 'getFights',
}

export enum EMessageTargets {
  Fight = 'fight',
}

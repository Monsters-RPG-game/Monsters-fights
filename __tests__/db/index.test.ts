import { describe, expect, it } from '@jest/globals';

describe('Fights', () => {
  describe('Should throw', () => {
    describe('No data passed', () => {
      it('This is example file for ci/cd to pass. Will add db tests after', () => {
        expect(2 + 2).toEqual(4);
      });
    });
  });
});

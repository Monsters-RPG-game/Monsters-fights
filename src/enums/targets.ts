export enum EFightsTargets {
  Attack = 'attack',
  CreateFight = 'createFight',
  Leave = 'leave',
  GetLogs = 'getLogs',
  GetFights = 'getFights',
}

export enum EMessageTargets {
  Fight = 'fight',
}
